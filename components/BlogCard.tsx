import React from 'react';

const BlogCard = ({
    title,
    thumbnail,
    link,
    date
                  }: {title: string, thumbnail: string, link: string, date: string}) => {
    return (
        <div>
            Blog Card
        </div>
    );
};

export default BlogCard;